# taskqueue 配置
#
# BASE_DIR 是每台机器自己的：由首次安装时的 setup.sh --base-dir 决定。
# deploy.sh 沿用本机 /etc/taskqueue.conf 里已有的值，不会用下面这行覆盖它；
# 这里的值仅在“本机还没有 /etc/taskqueue.conf”时作为新装兜底。
BASE_DIR="/var/lib/taskqueue"

# 以下键随部署同步到本机
MAX_CONCURRENT=15

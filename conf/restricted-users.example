# 受限用户名单示例（每行一个用户名）
# ---------------------------------------------------------------------------
# 名单里的用户会被从 HwHiAiUser 组移除，无法直接访问 /dev/davinci* NPU 设备，
# 必须通过 task-submit 提交任务；task-daemon 再以其身份 + 临时注入 HwHiAiUser
# 补充组来执行（见 task-daemon.sh 的 runuser --supp-group HwHiAiUser）。
#
# 部署：复制本文件为 conf/restricted-users 并填入真实用户名，然后 sudo bash deploy.sh。
# 真实名单已被 .gitignore 排除，不入库。
# ---------------------------------------------------------------------------
alice
bob
carol

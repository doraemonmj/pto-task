# 本地备份，历史版本用 git log 追溯即可
*.bak
*.bak.*

# 当前服务器的私有配置。
config/local.conf

# Source-tree runtime.  It is created locally when running the scripts from
# this checkout and must never be deployed or committed.
runtime/

# 本地问题跟踪：含未修复漏洞的利用细节，不入库
KNOWN_ISSUES.md
